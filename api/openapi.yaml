openapi: 3.1.0
info:
  title: JobDock API
  version: 0.1.0
  description: Versioned control-plane API for JobDock users, agents, and running jobs.
servers:
  - url: /api/v1
security:
  - sessionCookie: []
paths:
  /auth/login:
    post:
      security: []
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LoginRequest'}
      responses:
        '200': {description: Authenticated session, content: {application/json: {schema: {$ref: '#/components/schemas/Session'}}}}
        '401': {$ref: '#/components/responses/Problem'}
  /auth/me:
    get:
      operationId: currentUser
      responses:
        '200': {description: Current session, content: {application/json: {schema: {$ref: '#/components/schemas/Session'}}}}
  /jobs:
    get:
      operationId: listJobs
      responses:
        '200': {description: Visible jobs, content: {application/json: {schema: {type: object, properties: {items: {type: array, items: {$ref: '#/components/schemas/Job'}}}}}}}
    post:
      operationId: createJob
      parameters: [{$ref: '#/components/parameters/CSRFToken'}, {$ref: '#/components/parameters/IdempotencyKey'}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/JobSpec'}}}
      responses:
        '201': {description: Queued job, content: {application/json: {schema: {$ref: '#/components/schemas/Job'}}}}
        '422': {$ref: '#/components/responses/Problem'}
  /jobs/{jobId}:
    parameters: [{$ref: '#/components/parameters/JobID'}]
    get:
      operationId: getJob
      responses:
        '200': {description: Job detail, content: {application/json: {schema: {$ref: '#/components/schemas/Job'}}}}
    delete:
      operationId: deleteJob
      parameters: [{$ref: '#/components/parameters/CSRFToken'}, {$ref: '#/components/parameters/IdempotencyKey'}]
      responses: {'202': {description: Deletion accepted}, '409': {$ref: '#/components/responses/Problem'}}
  /jobs/{jobId}/stop:
    post:
      operationId: stopJob
      parameters: [{$ref: '#/components/parameters/JobID'}, {$ref: '#/components/parameters/CSRFToken'}, {$ref: '#/components/parameters/IdempotencyKey'}]
      responses: {'202': {description: Stop accepted}, '409': {$ref: '#/components/responses/Problem'}}
  /jobs/{jobId}/events:
    get:
      operationId: listJobEvents
      parameters: [{$ref: '#/components/parameters/JobID'}, {name: after, in: query, schema: {type: integer, format: int64}}]
      responses: {'200': {description: Ordered job events}}
  /jobs/{jobId}/stream:
    get:
      operationId: streamJob
      parameters: [{$ref: '#/components/parameters/JobID'}]
      responses: {'200': {description: Resumable text/event-stream}}
  /jobs/{jobId}/logs/{stream}/tail:
    get:
      operationId: tailJobLogs
      parameters:
        - {$ref: '#/components/parameters/JobID'}
        - {name: stream, in: path, required: true, schema: {type: string, enum: [stdout, stderr]}}
        - {name: after, in: query, schema: {oneOf: [{type: integer, format: int64, minimum: 0}, {type: string, const: tail}], default: tail}}
        - {name: Last-Event-ID, in: header, description: Last acknowledged byte offset used for automatic SSE reconnection, schema: {type: integer, format: int64, minimum: 0}}
      responses:
        '200': {description: Byte-offset resumable log chunks as text/event-stream}
        '400': {$ref: '#/components/responses/Problem'}
  /jobs/stream:
    get:
      operationId: streamOwnedJobs
      parameters:
        - {name: scope, in: query, schema: {type: string, enum: [mine], default: mine}}
        - {name: after, in: query, schema: {type: integer, format: int64}}
      responses: {'200': {description: Resumable owner-scoped job status text/event-stream}}
  /jobs/{jobId}/archive.zip:
    get:
      operationId: downloadJobArchive
      parameters: [{$ref: '#/components/parameters/JobID'}]
      responses: {'200': {description: Streaming ZIP archive, content: {application/zip: {schema: {type: string, format: binary}}}}}
  /nodes:
    get:
      operationId: listNodes
      responses:
        '200':
          description: Shared compute inventory with current reservations
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties: {items: {type: array, items: {$ref: '#/components/schemas/Node'}}}
  /nodes/{nodeId}:
    patch:
      operationId: updateNodeMetadata
      parameters:
        - {$ref: '#/components/parameters/NodeID'}
        - {$ref: '#/components/parameters/CSRFToken'}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, labels]
              properties:
                name: {type: string, minLength: 1, maxLength: 128}
                labels: {type: object, maxProperties: 64, additionalProperties: {type: string, maxLength: 256}}
      responses:
        '200': {description: Effective node metadata updated}
        '403': {description: Administrator role required}
        '422': {description: Invalid node metadata}
  /audit:
    get:
      operationId: listAuditEvents
      responses: {'200': {description: Administrator-only audit events}}
  /nodes/enrollment-tokens:
    post:
      operationId: createEnrollmentToken
      parameters: [{$ref: '#/components/parameters/CSRFToken'}]
      responses: {'201': {description: One-time token expiring in 15 minutes}}
  /secrets:
    get:
      operationId: listSecrets
      responses: {'200': {description: Secret metadata without values}}
    post:
      operationId: createSecret
      parameters: [{$ref: '#/components/parameters/CSRFToken'}]
      responses: {'201': {description: Encrypted secret metadata}}
  /agent/enroll:
    post:
      security: []
      operationId: enrollAgent
      responses: {'201': {description: Node ID and rotatable agent credential}, '401': {$ref: '#/components/responses/Problem'}}
  /agent/assignments/next:
    get:
      security: [{bearerToken: []}]
      operationId: nextAssignment
      responses: {'200': {description: Assignment and cooperative stop requests}}
  /agent/credential/rotate:
    post:
      security: [{bearerToken: []}]
      operationId: rotateAgentCredential
      responses: {'200': {description: New revocable agent credential}}
  /job-context/progress:
    post:
      security: [{bearerToken: []}]
      operationId: reportProgress
      responses: {'202': {description: Progress accepted}}
components:
  securitySchemes:
    sessionCookie: {type: apiKey, in: cookie, name: jobdock_session}
    bearerToken: {type: http, scheme: bearer}
  parameters:
    JobID: {name: jobId, in: path, required: true, schema: {type: string, format: uuid}}
    NodeID: {name: nodeId, in: path, required: true, schema: {type: string, format: uuid}}
    CSRFToken: {name: X-CSRF-Token, in: header, required: true, schema: {type: string}}
    IdempotencyKey: {name: Idempotency-Key, in: header, required: false, schema: {type: string, minLength: 16, maxLength: 128}}
  responses:
    Problem:
      description: RFC 9457-style problem detail
      content: {application/json: {schema: {$ref: '#/components/schemas/Problem'}}}
  schemas:
    LoginRequest:
      type: object
      required: [username, password]
      additionalProperties: false
      properties: {username: {type: string}, password: {type: string, format: password}}
    Session:
      type: object
      required: [user, csrf_token]
      properties: {user: {$ref: '#/components/schemas/User'}, csrf_token: {type: string}}
    User:
      type: object
      required: [id, username, role, created_at]
      properties:
        id: {type: string, format: uuid}
        username: {type: string}
        role: {type: string, enum: [admin, member]}
        created_at: {type: string, format: date-time}
    JobSpec:
      type: object
      additionalProperties: false
      required: [name, image, command, resources]
      properties:
        name: {type: string, minLength: 3, maxLength: 120}
        image: {type: string, maxLength: 512}
        command: {type: array, minItems: 1, maxItems: 128, items: {type: string}}
        working_directory: {type: string}
        environment: {type: object, additionalProperties: {type: string}}
        secret_refs: {type: array, items: {$ref: '#/components/schemas/SecretRef'}}
        registry_secret: {type: string}
        resources: {$ref: '#/components/schemas/Resources'}
        labels: {type: object, additionalProperties: {type: string}}
        node_selector: {type: object, additionalProperties: {type: string}}
    SecretRef:
      type: object
      required: [name, target, mode]
      properties: {name: {type: string}, target: {type: string}, mode: {type: string, enum: [file, env]}}
    Resources:
      type: object
      required: [cpu_millis, memory_bytes, gpu]
      properties:
        cpu_millis: {type: integer, format: int64, minimum: 1}
        memory_bytes: {type: integer, format: int64, minimum: 1}
        gpu:
          type: object
          required: [count, min_vram_bytes]
          properties: {count: {type: integer, minimum: 0}, min_vram_bytes: {type: integer, format: int64, minimum: 0}}
    Job:
      type: object
      required: [id, owner_id, spec, status, desired_status, observed_status, created_at, version]
      properties:
        id: {type: string, format: uuid}
        owner_id: {type: string, format: uuid}
        spec: {$ref: '#/components/schemas/JobSpec'}
        status: {$ref: '#/components/schemas/JobStatus'}
        desired_status: {$ref: '#/components/schemas/JobStatus'}
        observed_status: {$ref: '#/components/schemas/JobStatus'}
        assigned_node_id: {type: string, format: uuid}
        image_digest: {type: string}
        exit_code: {type: integer}
        queue_reason_code: {type: string}
        queue_reason: {type: string}
        failure_reason: {type: string}
        created_at: {type: string, format: date-time}
        started_at: {type: string, format: date-time}
        finished_at: {type: string, format: date-time}
        version: {type: integer, format: int64}
    JobStatus: {type: string, enum: [QUEUED, ASSIGNED, PULLING_IMAGE, STARTING, RUNNING, STOPPING, CANCELLED, SUCCEEDED, FAILED, LOST, DELETING, DELETED]}
    GPU:
      type: object
      required: [uuid, model, vram_bytes, allocated]
      properties:
        uuid: {type: string}
        model: {type: string}
        vram_bytes: {type: integer, format: int64}
        allocated: {type: boolean}
    GPUDiscovery:
      type: object
      required: [status]
      properties:
        status: {type: string, enum: [available, unavailable, disabled, unknown]}
        error_code: {type: string}
        message: {type: string}
    Node:
      type: object
      required: [id, name, status, cpu_total_millis, cpu_allocated_millis, memory_total_bytes, memory_allocated_bytes, workspace_free_bytes, labels, gpus, gpu_discovery, last_heartbeat]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        status: {type: string, enum: [ONLINE, OFFLINE, DEGRADED, DRAINING]}
        agent_version: {type: string}
        architecture: {type: string}
        docker_version: {type: string}
        cpu_total_millis: {type: integer, format: int64}
        cpu_allocated_millis: {type: integer, format: int64}
        memory_total_bytes: {type: integer, format: int64}
        memory_allocated_bytes: {type: integer, format: int64}
        workspace_free_bytes: {type: integer, format: int64}
        labels: {type: object, additionalProperties: {type: string}}
        gpus: {type: array, items: {$ref: '#/components/schemas/GPU'}}
        gpu_discovery: {$ref: '#/components/schemas/GPUDiscovery'}
        last_heartbeat: {type: string, format: date-time}
    JobUpdate:
      type: object
      required: [cursor, job_id, name, status, version, created_at]
      properties:
        cursor: {type: integer, format: int64}
        job_id: {type: string, format: uuid}
        name: {type: string}
        status: {$ref: '#/components/schemas/JobStatus'}
        version: {type: integer, format: int64}
        created_at: {type: string, format: date-time}
    Problem:
      type: object
      required: [type, title, status, code, detail]
      properties: {type: {type: string, format: uri}, title: {type: string}, status: {type: integer}, code: {type: string}, detail: {type: string}}
