import { useState } from "react";import { useQuery } from "@tanstack/react-query";import { Search } from "lucide-react";
import { api } from "@/api";import { JobTable } from "@/components/job-table";import { Input } from "@/components/ui/input";import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";
export function Jobs(){const [search,setSearch]=useState("");const [status,setStatus]=useState("all");const jobs=useQuery({queryKey:["jobs"],queryFn:api.jobs,refetchInterval:5000});const filtered=(jobs.data??[]).filter(j=>(status==="all"||j.status===status)&&(`${j.spec.name} ${j.id} ${j.spec.image}`.toLowerCase().includes(search.toLowerCase())));return <div className="space-y-4"><h1 className="text-xl font-semibold">Jobs</h1><div className="flex max-w-xl gap-2"><div className="relative flex-1"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground"/><Input className="pl-9" placeholder="Search jobs" value={search} onChange={e=>setSearch(e.target.value)}/></div><Select value={status} onValueChange={setStatus}><SelectTrigger className="w-40"><SelectValue/></SelectTrigger><SelectContent>{["all","QUEUED","RUNNING","SUCCEEDED","FAILED","CANCELLED","LOST"].map(s=><SelectItem key={s} value={s}>{s}</SelectItem>)}</SelectContent></Select></div><JobTable jobs={filtered}/></div>}
