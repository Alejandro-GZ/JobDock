import { useQuery } from "@tanstack/react-query";import { api } from "@/api";import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table";import { relative } from "@/lib/utils";
export function Audit(){const query=useQuery({queryKey:["audit"],queryFn:api.audit});return <div className="space-y-4"><h1 className="text-xl font-semibold">Audit</h1><div className="overflow-hidden rounded-md border"><Table><TableHeader><TableRow><TableHead>Action</TableHead><TableHead>Target</TableHead><TableHead>Actor</TableHead><TableHead>Metadata</TableHead><TableHead>Time</TableHead></TableRow></TableHeader><TableBody>{(query.data??[]).map(e=><TableRow key={e.id}><TableCell className="font-mono text-xs">{e.action}</TableCell><TableCell>{e.target_type} · {e.target_id.slice(0,12)}</TableCell><TableCell className="font-mono text-xs">{e.actor_id?.slice(0,12)??"system"}</TableCell><TableCell className="max-w-xs truncate text-xs text-muted-foreground">{JSON.stringify(e.metadata)}</TableCell><TableCell>{relative(e.created_at)}</TableCell></TableRow>)}</TableBody></Table></div></div>}
