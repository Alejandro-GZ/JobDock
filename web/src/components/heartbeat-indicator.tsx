import{HeartPulse}from"lucide-react";import{Tooltip,TooltipContent,TooltipTrigger}from"@/components/ui/tooltip";import{cn,relative}from"@/lib/utils";const colors:Record<string,string>={ONLINE:"text-emerald-500",DEGRADED:"text-yellow-500",DRAINING:"text-blue-500",OFFLINE:"text-red-500"};export function HeartbeatIndicator({status,lastHeartbeat,size="md"}:{status:string;lastHeartbeat:string;size?:"sm"|"md"}){return <Tooltip><TooltipTrigger asChild><span tabIndex={0} className="inline-grid place-items-center rounded-full outline-none focus-visible:ring-2 focus-visible:ring-ring"><HeartPulse key={lastHeartbeat} aria-label={`${status}, last heartbeat ${relative(lastHeartbeat)}`} className={cn(size==="sm"?"size-4":"size-5",colors[status]??"text-muted-foreground",status!=="OFFLINE"&&"animate-heartbeat")}/></span></TooltipTrigger><TooltipContent><p className="font-medium">{status}</p><p>Last heartbeat: {new Date(lastHeartbeat).toLocaleString()}</p><p>{relative(lastHeartbeat)}</p></TooltipContent></Tooltip>}
