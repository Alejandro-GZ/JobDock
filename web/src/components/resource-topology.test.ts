import { describe, expect, it } from "vitest";import { buildTopology } from "./resource-topology";import type { Job, Node } from "@/types";
const node:Node={id:"node",name:"worker",status:"ONLINE",agent_version:"test",architecture:"amd64",docker_version:"test",cpu_total_millis:1000,cpu_allocated_millis:0,memory_total_bytes:1000,memory_allocated_bytes:0,workspace_free_bytes:1000,labels:{},gpus:[],gpu_discovery:{status:"available"},last_heartbeat:"2026-01-01T00:00:00Z"};
const job=(id:string,status="RUNNING"):Job=>({id,owner_id:"owner",status,desired_status:status,observed_status:status,created_at:"2026-01-01T00:00:00Z",finished_at:status==="RUNNING"?undefined:"2026-01-01T00:01:00Z",version:1,spec:{name:id,image:"alpine",command:[],environment:{},secret_refs:[],resources:{cpu_millis:1,memory_bytes:1,gpu:{count:0,min_vram_bytes:0}},labels:{},node_selector:{}}});
describe("resource topology",()=>{it("connects resources through Dock to selected jobs",()=>{const graph=buildTopology([node],[job("active"),job("done","SUCCEEDED")],"owner",{});expect(graph.nodes.map(item=>item.id)).toEqual(["resource-node","dock","job-active","job-done"]);expect(graph.edges).toHaveLength(3)});it("does not include seen terminal jobs",()=>{const done=job("done","SUCCEEDED");const graph=buildTopology([node],[done],"owner",{done:done.finished_at!});expect(graph.nodes.map(item=>item.id)).toEqual(["resource-node","dock"])})});
