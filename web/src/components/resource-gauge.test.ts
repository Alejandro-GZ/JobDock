import{describe,expect,it}from"vitest";import{gaugePercent,gaugeTone}from"./resource-gauge";describe("resource gauge",()=>{it("clamps percentages",()=>{expect(gaugePercent(5,10)).toBe(50);expect(gaugePercent(20,10)).toBe(100);expect(gaugePercent(-1,10)).toBe(0);expect(gaugePercent(0,0)).toBeNull()});it("maps utilization colors",()=>{expect(gaugeTone(20)).toContain("emerald");expect(gaugeTone(60)).toContain("yellow");expect(gaugeTone(80)).toContain("orange");expect(gaugeTone(95)).toContain("red")})});
